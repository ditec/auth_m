%p
  %strong Name:
  = @management.name
%p
  %h3 Recursos
  %table
    -@management.resources.each do |resource|
      %tr
        %td= resource.name

= link_to 'Edit', edit_management_path(@management)
= link_to 'Back', managements_path
