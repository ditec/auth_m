<h1>New User</h1>
<%= form_with model:@user, url: person_create_user_path,  local: true do |form| %>
  <%= render partial: 'form', locals: {form: form} %>
  <p>
    <%= form.submit %>
  </p>
<% end %>
<%= link_to 'Back', person_path(@person) %>
